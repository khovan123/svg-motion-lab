{
  "name": "svg-motion-lab",
  "private": true,
  "version": "0.3.0",
  "scripts": {
    "build": "node compile.js --manifest motion-manifest.json",
    "build:example": "node compile.js --manifest fixtures/motion-manifest.example.json",
    "test": "node test/compiler.test.js"
  }
}
