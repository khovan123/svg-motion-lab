{
  "name": "svg-motion-lab",
  "private": true,
  "version": "0.3.0",
  "scripts": {
    "start": "npx serve web",
    "dev": "npx serve web",
    "build": "node compile.js --manifest motion-manifest.json && node fix-transforms.js dist/animation.html",
    "build:example": "node compile.js --manifest fixtures/motion-manifest.example.json",
    "test": "node test/compiler.test.js"
  },
   "dependencies": {
    "jsdom": "^24.1.0"
   }
}
